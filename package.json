{
  "name": "content-list",
  "version": "1.0.0",
  "description": "WordPress Content List plugin",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build src/main.scss --out-dir dist --no-cache --no-source-maps"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "autoprefixer": "^9.7.5",
    "parcel-bundler": "^1.12.4",
    "postcss-modules": "^1.5.0",
    "sass": "^1.26.3"
  }
}
